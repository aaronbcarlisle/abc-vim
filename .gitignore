# Vim runtime scratch dirs. The .vimrc creates these on startup, so they do not
# need to be tracked (git cannot track empty directories anyway).
backup/
swap/
undo/

# Vundle and other plugins are installed at runtime, not committed.
bundle/
